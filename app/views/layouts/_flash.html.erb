<div class="container">
  <div class="row">
    <div class="col-md-12">
      <% flash.each do |type, messages| %>
        <%= flash_messages(type, messages) %>
      <% end %>
    </div>
  </div>
</div>
